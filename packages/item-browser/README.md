This module is not a full site on its own. It is only used to allow for easier reuse
of CSS and JS parts on the item browser site. The item browser imports this as a git
submodule and copies specific files out.